\documentclass[english,12pt,a4paper,elec,utf8,online]{aaltothesis}

\usepackage{iftex}
\ifXeTeX
  \usepackage{fontspec}
  \usepackage{xeCJK}
  \setmainfont{Times New Roman}
  \setCJKmainfont{SimSun}
\else
  \usepackage{CJKutf8}
\fi
\usepackage{array}
\usepackage{booktabs}
\usepackage[backend=bibtex,style=numeric]{biblatex}
\usepackage{xurl}
\addbibresource{thesisreferences.bib}

\urlstyle{tt}
\DeclareRobustCommand{\codepath}[1]{\nolinkurl{#1}}
\pdfstringdefDisableCommands{\def\codepath#1{#1}}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\degreeprogram{Electronics and Nanotechnology}
\major{Microelectronic Circuit Design}
\univdegree{MSc}
\thesisauthor{Linan Jiang}
\thesistitle{基于证据的 Mie 散射喷雾视频处理与穿深预测}
\thesissubtitle{从标定与特征提取到基线模型和不确定性感知代理模型}
\place{Espoo}
\date{7 April 2026}
\supervisor{Prof.\ Ossi Kaario}
\advisor{Dr Zeeshan Ahmad}
\advisor{Dr Qiang Cheng}
\collaborativepartner{Wartsila}
\keywords{Mie 散射\spc 喷雾成像\spc 穿深\spc 计算机视觉\spc surrogate 建模}
\thesisabstract{本文提出一套计算流程，用于预测多燃料船用发动机中预喷柴油喷雾的穿深演化，并为早期撞壁风险筛查提供几何输入。研究目标是在设计早期支持对液态柴油与活塞或缸壁表面相互作用的工程评估，因为这类相互作用可能导致机油稀释、壁面润湿以及混合恶化。该流程建立在室温光学喷雾燃烧舱中的 Mie 散射高速成像实验之上。数据由固定相机布置下、同一喷射器在多种工况和喷嘴几何参数下采集的 TB 级 12 bit 灰度视频构成。首先通过人工标定确定喷嘴中心和像素到物理长度的映射。随后利用 CUDA 加速的处理流程，从原始图像序列中提取按 plume 分解的时序观测量和边界演化。为构造适合监督学习的目标，本文以降阶经验拟合表示穿深轨迹，用以缓解有限观测窗口带来的右删失、改善不同工况间的时间对齐，并构造更具代表性的穿深目标。在此基础上，建立了多阶段神经网络 surrogate：先在中位数穿深轨迹上训练确定性基线模型，再在全部经筛选的拟合轨迹上进行异方差学习，最后在原始时序观测可靠处引入直接监督，并在观测稀疏区间保留教师模型的保守引导，从而完成教师引导的细化训练。这一分阶段设计可有效避免直接对右删失、标签不完整的原始观测进行单阶段训练时所产生的系统性低估偏差。网络输出的均值与不确定性随后可与简化喷雾角假设和缸内掩膜组合，作为概率化撞壁筛查的原型路径。本文的主要结果是一套面向工程的 surrogate workflow，将大规模光学喷雾成像连接到穿深预测和几何风险筛查。在来自 9 个喷嘴方案的约 15,000 条穿深轨迹（约 40 万个标注时间步）上评估，细化后的代理模型总体 RMSE 为 6.9 mm（约为实测穿深量程的 7.2%），异方差预测区间校准至标称覆盖率（1sigma 覆盖率 68.5%，2sigma 覆盖率 95.7%）。该方法旨在作为前端筛查工具，而不是完整的缸内喷雾物理真值模型。其当前范围仍局限于室温、非反应数据和简化碰撞几何，但为未来引入物理正则和更高维发动机分析提供了基础。}

\begin{document}
\ifXeTeX
\else
\begin{CJK*}{UTF8}{gbsn}
\fi

\makecoverpage
\makecopyrightpage

\begin{abstractpage}
本文提出一套计算流程，用于预测多燃料船用发动机中预喷柴油喷雾的穿深演化，并为早期撞壁风险筛查提供几何输入。研究目标是在设计早期支持对液态柴油与活塞或缸壁表面相互作用的工程评估，因为这类相互作用可能导致机油稀释、壁面润湿以及混合恶化。

该流程建立在室温光学喷雾燃烧舱中的 Mie 散射高速成像实验之上。数据由固定相机布置下、同一喷射器在多种工况和喷嘴几何参数下采集的 TB 级 12 bit 灰度视频构成。首先通过人工标定确定喷嘴中心和像素到物理长度的映射。随后利用 CUDA 加速的处理流程，从原始图像序列中提取按 plume 分解的时序观测量和边界演化。

为构造适合监督学习的目标，本文以降阶经验拟合表示穿深轨迹，用以缓解有限观测窗口带来的右删失、改善不同工况间的时间对齐，并构造更具代表性的穿深目标。在此基础上，建立了多阶段神经网络 surrogate：先在中位数穿深轨迹上训练确定性基线模型，再在全部经筛选的拟合轨迹上进行异方差学习，最后在原始时序观测可靠处引入直接监督，并在观测稀疏区间保留教师模型的保守引导，从而完成教师引导的细化训练。这一分阶段设计可有效避免直接对右删失、标签不完整的原始观测进行单阶段训练时所产生的系统性低估偏差。网络输出的均值与不确定性随后可与简化喷雾角假设和缸内掩膜组合，作为概率化撞壁筛查的原型路径。

本文的主要结果是一套面向工程的 surrogate workflow，将大规模光学喷雾成像连接到穿深预测和几何风险筛查。在来自 9 个喷嘴方案的约 15,000 条穿深轨迹（约 40 万个标注时间步）上评估，细化后的代理模型总体 RMSE 为 6.9~mm（约为实测穿深量程的 7.2\%），异方差预测区间校准至标称覆盖率（1σ 覆盖率 68.5\%，2σ 覆盖率 95.7\%）。该方法旨在作为前端筛查工具，而不是完整的缸内喷雾物理真值模型。其当前范围仍局限于室温、非反应数据和简化碰撞几何，但为未来引入物理正则和更高维发动机分析提供了基础。
\end{abstractpage}

\mysection{前言}
感谢 Ossi Kaario 教授的指导，感谢 Zeeshan Ahmad 博士与 Qiang Cheng 博士在喷雾成像和建模方面提供的技术支持，也感谢围绕喷雾处理流程开展协作的同事们帮助我不断明确这项工作的工程方向。

\thesistableofcontents
\dothesispagenumbering{}

\section{引言}



\subsection{研究意义}

国际航运业正同时面临减排压力、燃料多样化以及运行可靠性的共同要求。IMO 于 2023 年发布的温室气体战略不仅继续提出碳强度下降目标，也提出了到 2030 年零或近零排放能源占比的目标，这显著加速了替代燃料推进系统的工程化兴趣 \cite{imo2023ghgstrategy}。行业跟踪也表明，液化天然气、甲醇、氨等替代燃料已经从概念讨论进入了具体船型与订单决策阶段 \cite{classnk2025alternativefuels}。

在这一转型背景下，双燃料和多燃料船用发动机不仅依赖燃料供给本身，也依赖可靠点火和稳定燃烧。对于许多这类发动机，小剂量预喷燃油负责为主燃料提供点火源。例如，在低压燃气工况下，WinGD X-DF 发动机使用少量 pilot fuel 实现点火，并且在燃气供应丢失时可以退回柴油模式 \cite{wingd_xdf_manual_2021,wingd_lng_faq_2025}。类似逻辑也适用于甲醇和氨发动机方案，因为这些主燃料在相同条件下并不容易自燃，因此仍需要 pilot fuel \cite{classnk_methanol_2024,wingd_ammonia_faq_2025}。

然而，预喷柴油喷雾不仅仅是点火辅助手段，它的空间发展同样重要。如果液态燃料在形成有效点火核之前就到达活塞顶或缸壁，则可能导致壁面润湿、燃烧恶化、排放升高以及机油稀释 \cite{peraza2022swi,ahmad2025fueldilution}。因此，对于喷油器开发和设计早期筛选而言，在直接进入高成本发动机试验或高保真燃烧模拟之前，先估计液相喷雾包络以及潜在撞壁风险具有明确价值。

\subsection{当前工程实践}

在工程实践中，喷雾行为与撞壁风险通常通过三类路径评估：发动机试验、高保真 CFD 与喷雾燃烧仿真，以及光学喷雾实验。发动机试验最接近真实工况，但成本高、速度慢，而且很难在一次试验中只隔离一个喷雾设计因素。高保真仿真能够给出更丰富的内部流动信息，但它需要复杂的模型选择、大量输入假设和显著计算资源 \cite{liu2020sprayreview,peraza2022swi}。这种成本并不只是定性判断。燃烧计算文献中引用的一个反应柴油喷雾 LES 例子显示，单个高分辨率 realization 使用最高 2200 万网格和 54 组分 \(n\)-dodecane 机理，仅计算喷射开始后 \SI{2}{ms}，就需要约 48,000 个 CPU core-hours，并且最长 wall-clock 时间达到 20 天 \cite{CurtisNiemeyerSung2017}。由于 TFLOP/s 强烈依赖硬件和求解器效率，core-hours 是更可复现的成本指标；即使用保守的逐核吞吐量粗略换算，这一例子也已经达到单次 realization 的 petaflop-order 算术工作量，更不用说大规模工况扫描。

光学实验则扮演不同角色。它们能提供受控且可重复的观测，适合对大量候选喷嘴、设定或工况进行比较。但光学数据并不会自动转化为工程决策。尤其是，喷雾成像中观察到的液相边界依赖于所用诊断方法以及后处理选择。ECN 的说明明确指出，液相穿深测量具有方法依赖性，光学边界应被理解为实验定义下的量，而不是隐藏真实物理场的直接真值 \cite{ecn_liquid_penetration,ecn_liquid_penetration_accessory}。

\subsection{现有解决方案}

柴油喷雾研究已经提供了大量实验和建模方法。实验方面，高速成像和光学诊断广泛用于表征穿深、扩张角和喷雾形态。建模方面，经验关联式、现象学模型以及基于 CFD 的方法常用于描述喷雾发展 \cite{Linne2013SprayImaging,HiroyasuArai1990,NaberSiebers1996,liu2020sprayreview}。这些方法为喷油器设计和燃烧系统分析打下了重要基础。

但对本文关注的应用场景而言，每类方法都有局限。经验和现象学模型的优点是紧凑、可解释，但它们的假设往往在准稳态条件下最强，对于短脉冲、强瞬态或多次喷射情形则不够可靠 \cite{liu2020sprayreview}。此外，许多经典表达式本身是分段的或依赖工况切换，因此作为学习目标时并不光滑，不适合拟合含噪的短时 pilot spray 轨迹。高保真 CFD 虽然信息丰富，却通常不是设计早期大规模筛选的高效工具。近期 LES 研究也将计算成本视为核心设计约束；例如，一个 ducted-fuel-injection LES 研究报告称，相比更直接的采样策略，多切片 ensemble 方法在单个构型上节省了约 447,000 个 core-hours \cite{Segatori2023DFILES}。纯图像测量则只能描述特定光学布置下观察到的内容，不能单独提供一个用于工程比较的中间层预测模型。

近年来，机器学习方法开始作为经典经验和现象学模型的补充，出现在喷雾建模与燃烧 surrogate 文献中。这些方法表明，在无需封闭解析定律的情况下，学习回归同样可以复现喷雾宏观行为。然而，本文具体关注的一组约束尚未被这些工作直接针对：短脉冲 pilot injection 的瞬态阶段主导了可观测窗口、有限视野舱产生的右删失测量轨迹，以及面向下游几何风险筛查所必需的不确定性感知输出。通用数据驱动喷雾回归与本文具体需求之间的这一差距，正是本文专门构建 surrogate 工作流的动机所在。

\subsection{问题陈述}

因此，本文要解决的核心问题并不是柴油喷雾是否已经被研究，而是能否在光学实验和高成本验证之间建立一个实际可用的中间层模型。更具体地说，当前缺少一种快速、可重复的方法，用于预测预喷柴油非反应液相包络随时间的演化，从而为后续撞壁风险分析提供有依据的几何输入。本文目标不是替代 CFD 或发动机试验，而是在光学实验与这些高成本验证路径之间加入一层 surrogate layer。

这一缺口之所以重要，是因为短脉冲 pilot injection 具有很强瞬态性。喷雾角和穿深并不总能被视为固定常数，瞬态扩散行为会进一步影响穿深和混合 \cite{JungManinSkeenPickett2015}。与此同时，喷雾撞壁在物理上仍然重要且尚未被完全解析，尤其是在从实验室诊断走向发动机相关后果，如壁膜形成和可靠性问题时更是如此 \cite{peraza2022swi}。对于船用发动机，这一问题还因几何尺度而更加复杂，因为大缸径工况与熟悉的汽车喷雾撞壁图景并不完全一致 \cite{liu2024marine_impingement}。当前真正缺失的不是另一个孤立图像测量，而是一个在进入完整反应喷雾或整机分析之前、能够支持早期风险筛查的实用预测器。

一个有前景的思路是直接从大规模光学喷雾数据中学习这一预测器。核心观察在于：Mie 散射实验已经在一定工况范围内捕获了相关液相包络行为——缺少的是将这些观测转化为快速、可参数化预测模型的方法。以测量穿深轨迹的降阶拟合为训练目标、数据驱动的 surrogate 模型恰好填补了原始实验测量与基于物理仿真之间的空白：它保留了光学数据的经验保真度，同时具备工程筛查所需的泛化能力与计算效率。通过直接从喷射工况到穿深轨迹参数学习映射关系，该 surrogate 模型绕开了对封闭解析喷雾定律的依赖，并可随数据集扩展自然更新。引入不确定性量化进一步使输出结果在风险评估中具备可辩护性，而非仅提供无法反映可靠性的单一确定性预测。这一需求并不仅是概念层面的。Mie 散射实验本身存在 shot-to-shot 随机性，光学边界提取依赖于对阈值的选择，其敏感程度因工况而异，而每个工况下可用的重复喷射次数也受到归档规模的限制。这些因素共同决定了穿深标签中的偶然不确定性不可忽视。仅输出条件均值的模型无法告知该均值对特定工况是否可靠，而在风险筛查场景中，一个可以与几何碰撞准则并列解读的校准预测区间，比过度自信的点估计更具实际价值 \cite{KendallGal2017}。

\subsection{研究目标}

基于上述问题，本文旨在开发一个用于预喷柴油的、数据驱动的非反应液相包络预测模型。其目标是从喷射工况与时间信息出发，预测喷雾包络和穿深趋势，并给出适于工程解释的不确定性描述。

本文并不声称完整解决真实发动机中的壁面润湿或反应喷雾撞壁问题。相反，本文将得到的模型定位为船用双燃料发动机开发中早期撞壁风险筛查的前端 surrogate。换句话说，目标贡献是一个实用工程工具，用于缩小候选设计空间并支撑后续更高保真验证，而不是替代 CFD 或发动机试验。

\subsection{研究方法}

为实现这一目标，本文使用室温加压空气舱中的柴油喷雾高速可视化实验。生成的图像序列经过处理，提取与穿深和液相包络相关的几何特征，构成后续建模数据集。

在此基础上，本文使用数据驱动回归来学习实验观测中的液相包络时变行为。该模型被视为 surrogate function approximator，而不是新的闭式喷雾公式。其输出随后将用于几何风险映射，也就是估计某个工况下喷雾是否更有可能接近潜在撞壁区域。在整个流程中，提取出的边界始终被解释为所选诊断方法下的光学液相包络定义，而不是缸内液相场的绝对真值 \cite{ecn_liquid_penetration}。为评估 surrogate 是否在现有解析近似之外提供了额外价值，本文还将预测穿深趋势与 Hiroyasu--Arai 两段式模型等经典标度律参考值并置比较 \cite{HiroyasuArai1990}，从而在有物理基础的基准之上解读数据驱动输出。

\subsection{为什么需要这样的工作流}

本文的工作流结构是刻意设计的，而不是出于实现便利。若直接从原始喷雾视频端到端学习穿深预测，将带来高计算成本、低可解释性，并且与当前归档数据规模不匹配。这样的端到端方式也难以审计本文问题中真正关键的中间选择，例如标定、光学边界定义、目标拟合以及不确定性感知回归。这一判断也符合更广泛的科学图像处理实践：Fiji/ImageJ、scikit-image、OpenPIV 和 PIVlab 等成熟开源生态都强调可脚本化、可审计、可复用的处理流程，而不是一次性的人工解释 \cite{Schindelin2012Fiji,VanDerWalt2014ScikitImage,OpenPIV,ThielickeSonntag2021PIVlab}。

此外，从方法论上也不应直接对原始强度体学习。Mie 散射图像并不提供液体体积分数的真值场，而是给出一种光学边界定义，其外观依赖照明、散射、背景和后处理选择 \cite{ecn_liquid_penetration,ecn_liquid_penetration_accessory}。因此，第一步必须先将原始视频转换成更稳定、按 plume 分解且具有明确物理解释的观测量，再进入后续建模。

有限观测窗口还带来额外复杂性。在当前实验布置中，可见径向范围有限，因此某些工况会产生右删失的穿深轨迹，其晚期演化无法被完整观测。若直接将这些不完整轨迹当作标签使用，学习目标会在最需要稳健外推的区域被系统性低估。因此，降阶拟合阶段不仅是一个平滑器，也是一个面向删失的目标构造步骤：它支持超出可见窗口的保守外推，改善不同工况间的时间对齐，并生成更适合异方差回归的目标。

\subsection{主要贡献}

本文的主要贡献如下：
\begin{enumerate}
  \item 提出一套 CUDA 加速工作流，将 TB 级 Mie 散射视频转换为按 plume 分解的时序观测量和边界轨迹；
  \item 提出一套降阶拟合策略，用于轨迹对齐、稳健外推以及缓解穿深目标中的右删失影响；
  \item 提出多阶段不确定性感知学习框架，将确定性 MSE 训练、异方差 NLL 训练以及针对部分观测原始轨迹的教师引导细化训练结合起来；
  \item 提出一种简化的概率化撞壁筛查方法，将穿深预测与下游几何碰撞评估解耦。
\end{enumerate}

\subsection{研究范围}

本文的研究范围局限于受控加压舱条件下、非反应、非蒸发的预喷柴油喷雾。研究关注的是在光学布置下观察到的宏观液相包络几何及其时间演化，而不是完整的缸内燃烧过程。

因此，燃烧化学、蒸发、双燃料点火耦合、真实活塞顶和缸壁的热相互作用，以及真实发动机壁膜真值预测均不在本文范围内。本文提出的方法应被理解为一种早期工程筛查 surrogate，而不是发动机壁面润湿的最终真值模型。

此外，本文提出的基于 MLP 的 surrogate 模型主要面向训练数据所覆盖工况范围内的插值预测。对于明显超出当前实验数据分布的外推工况，其预测精度与不确定性估计不在本文中作同等级保证。因此，该模型应被理解为数据覆盖范围内的工程筛查工具，而不是对任意未知工况都可靠的通用喷雾真值模型。

将范围限制在非反应、非蒸发条件下，是出于物理上有据可查的保守筛查考量。在决定液相包络最终到达范围的喷射初始阶段，主导物理过程是动量交换、气动阻力以及一次和二次破碎——这些过程在冷喷雾条件下同样完整存在。ECN 基准研究证实，在受控环境舱条件下测量的非反应液相穿深可跨实验室复现，并且与发动机相关标度律中出现的相同支配参数——环境密度、喷射压力和喷嘴几何——高度相关 \cite{NaberSiebers1996,ecn_liquid_penetration}。在冷喷雾条件下，液相长度达到其最大无抑制程度，因为在高温缸内条件下缩短液相包络的蒸发过程此时并不存在。因此，冷喷雾穿深为撞壁筛查提供了一个保守上界：若某工况的非反应液相包络未接近壁面边界，则在对应发动机工况下（蒸发进一步缩短液相长度），该工况发生撞壁的可能性更低。非反应研究范围支持具有可辩护性的保守筛查，而非使结论与发动机工况无关。

\subsection{论文结构}

本文接下来的结构如下。文献综述与技术依据部分将本研究置于柴油喷雾成像、光学诊断和 surrogate 建模背景下。研究材料与方法部分介绍实验数据、图像处理流程、特征提取、轨迹拟合和模型训练过程。结果部分报告已归档的处理资产和模型证据。结论部分总结当前结果、主要限制和后续开发需求。

\section{文献综述与技术依据}



\subsection{为什么在 Mie 成像中采用以穿深为核心的观测量}

Mie 散射适合用于可视化致密液相喷雾，因为它具有较高的时间分辨率，实验实现也相对简单 \cite{Linne2013SprayImaging}。同时，记录得到的强度场并不是燃料质量或液体体积分数的直接测量。散射响应取决于液滴尺寸、折射率、波长和观测几何，而在致密区域中还会出现多重散射和阴影效应 \cite{BohrenHuffman2004,Linne2013SprayImaging}。因此，试图直接从像素强度反演完整的喷雾物理状态，本质上是不适定问题。

因此，本文采取了实验喷雾分析中常见的实践立场：把强度视为几何线索的载体，而不是直接的热力学场。相较于声称获得精确液相分布，穿深、锥角、投影面积和边界位置这类操作性观测量更具可辩护性 \cite{Linne2013SprayImaging,LefebvreMcDonell2017}。在柴油喷雾研究中，穿深和锥角之所以长期作为标准描述符，正是因为它们可解释，并且在受控实验研究中相对稳健 \cite{Dent1971Penetration,HiroyasuArai1990,NaberSiebers1996}。

这种强调也与既有光学喷雾研究相一致。在光学柴油发动机内开展的二维 Mie 散射研究中，实际报告的往往是喷雾尖端穿深和锥角等图像分析结果，而不是试图重建完整的液相状态场 \cite{MuenchLeipertz1992}。更近期的成像研究即便在仪器配置和同步方式上更加复杂，液相喷雾比较仍然主要围绕穿深和扩张角等指标展开 \cite{JungManinSkeenPickett2015}。因此，现有文献支持这样的判断：基于 Mie 视频开展的论文，应主要根据其提取观测量的质量与稳健性来评估。

经典的喷雾穿深建模也解释了为什么穿深是如此中心的描述符。在广泛使用的Hiroyasu-Arai图景中，尖端穿深以阶段形式书写，一个分支在破碎之前，另一个在破碎之后。在简化比例形式中，
\[
S_{\mathrm{tip}}(t)\propto
\begin{cases}
t, & t<t_b,\\
t^{1/2}, & t\ge t_b,
\end{cases}
\]
其中\(t_b\)是破碎时间，前因子取决于压力差、喷嘴直径和流体特性\cite{HiroyasuArai1990}。这种类型的模型具有吸引力，因为它为总体穿深历史提供了可解释的低阶定律。然而，Zhou等人最近的起始注入瞬态分析表明，当储罐加压和针阀开启瞬态重要时，早期行为可能比经典两阶段定律更复杂。在该研究中，SOI阶段被发现大约以\(t^{3/2}\)最初，然后\(t^{3/4}\)，只有后来接近更熟悉的\(t^{1/2}\)行为\cite{ZhouLiYi2021}。这一观察直接与本论文相关：预喷柴油注入足够短，早期瞬态部分占据观察信号的不可忽视部分，因此单一刚性经验定律不总是充分描述整个轨迹。

结束注入研究进一步扩展了这一想法，表明相关的时间变量本身在不同阶段中会发生变化。在Zhou等人报告的EOI后尖端和尾端穿深的扩展公式中，前过渡分支仍然以ASOI时钟\(t\)为基准，但减速分支包括\((t-t_i)^{1/2}\)和\((t-t_i)^{1/4}\)项，其中\(t_i\)表示注入持续时间\cite{ZhouLiLaiWei2019}。其组合表达式对于液体长度尺度\(S_L(t)\)可以写成
\[
S_L(t)=
\begin{cases}
K_v \left( \dfrac{2\Delta P}{\rho_f} \right)^{1/2} t,
& 0 < t \le t_b,\\[6pt]
K_p \left( \dfrac{\Delta P}{\rho_a} \right)^{1/4} d_n^{1/2} t^{1/2},
& t_b < t \le t_i,\\[6pt]
K_p \left( \dfrac{\Delta P}{\rho_a} \right)^{1/4} d_n^{1/2} t^{1/2}
- k d_n^{1/2}(t-t_i)^{1/2}(\tan\theta)^{-1/2}\rho_a^{-1/4},
& t_i < t \le 2t_i,\\[6pt]
2^{1/2} K_p \left( \dfrac{\Delta P}{\rho_a} \right)^{1/4} d_n^{1/2} t_i^{1/4}(t-t_i)^{1/4}
- k d_n^{1/2}(t-t_i)^{1/2}(\tan\theta)^{-1/2}\rho_a^{-1/4},
& 2t_i < t.
\end{cases}
\]
在简化意义上，这表明穿深定律的一部分以喷射起始时刻为时间原点，而另一部分则以喷射结束之后的延迟时钟为索引，或者更准确地说，以减速卷吸波开始影响喷雾尖端的时刻为索引。这个区别对本文很重要，因为它说明仅仅讨论幂律指数并不足够，有效时间原点本身也承载物理意义。它同时也支持采用更柔性的工程参数化，因为在短预喷注入中，数据窗口可能同时覆盖这两种时钟，而精确的切换时刻并不能从带噪的 Mie 图像导出轨迹中被直接识别。

\subsection{来自计算机视觉与信号处理的支撑理论}

实现的图像处理链使用手动校准、对数域前景构建、基于梯度的增强、极坐标聚合、频谱对齐、逆变换和二值形状测量。这些步骤并非任意启发式；每个步骤都得到了计算机视觉和信号处理中确立的理论支持。

手动喷嘴校准是合理的，因为下游角度分析假设图像坐标中的固定喷嘴中心。校准任务是一个噪声圆拟合问题，最小二乘圆估计是标准工具\cite{Pratt1987CircleFit,Taubin1991CircleFit}。一旦知道中心，多孔喷雾可以相对于物理上有意义的原点描述，而不是通过随裁剪或眩光变化的不稳定图像坐标。

对数比预处理阶段在概念上与同态信号处理相关。当图像形成包含乘法成分时，应用对数将乘法变化转换为加法变化，之后背景减法和滤波变得更加稳定\cite{OppenheimSchaferStockham1968,GonzalezWoodsDIP}。本论文中的实现不声称是经典意义上的完整同态滤波器，但遵循了相同的理论动机：照明和增益变化应在测量喷雾几何之前衰减。

在前景构建之后，流程强调边缘并抑制扩散的低频背景结构。Sobel滤波器等梯度幅度算子是强调边界转换的标准工具，而百分位缩放、自动阈值和形态学是稳定分割在非均匀对比度下的标准方法\cite{GonzalezWoodsDIP,Szeliski2010Vision,ZackRogersLatt1977}。这对Mie视频特别相关，因为感兴趣的边界通常首先作为局部对比度变化而不是全局一致的绝对强度水平可见。

角度密度阶段可以解释为围绕喷嘴中心的极坐标重采样图像。在计算机视觉术语中，这将2D图像中的重复方向结构转换为角度轴上的一维信号，这更容易在帧和记录之间进行比较。一旦获得该角度信号，使用其谐波结构进行对齐是标准信号处理想法：周期性模式在傅里叶域中自然显现，而喷雾计数谐波的相位提供旋转偏移的紧凑估计\cite{CooleyTukey1965FFT}。同样，后续重映射阶段遵循标准逆变换理论，其中输出像素通过逆变换查询源坐标，使重采样条带密集且没有前向映射孔\cite{Szeliski2010Vision}。

工作流中稍后使用的降阶穿深拟合也基于标准估计理论。当使用参数曲线\(S(t;\theta)\)总结噪声观测\(y_i\)时，自然公式是非线性最小二乘，可能在少数样本受分割噪声、早期时间模糊或截断轨迹污染时使用鲁棒惩罚。Huber损失是二次最小二乘和绝对误差拟合之间的经典折衷：它在残差接近零时表现得像平方误差，在大残差时表现得像绝对误差，这使其对异常值不敏感，同时保持平滑优化\cite{Huber1964,NocedalWright2006}。这直接匹配\texttt{fit\_raw\_data.py}的角色，该程序使用鲁棒非线性拟合将噪声喷雾轨迹转换为后续学习阶段的紧凑参数。

这些理论支持本论文的设计选择：在任何下游建模之前，原始视频应转换为喷雾更容易比较、阈值和一致测量的坐标系和信号表示。

\subsection{高通量科学成像与 GPU 适配性}

本论文中的视频归档不是一组孤立的图像，而是密集时空张量的集合。在喷雾对齐之前，每个记录自然表示为视频立方体\(V\in\mathbb{R}^{F\times H\times W}\)。在旋转、裁剪、掩码和喷雾提取后，相同信息重新组织为喷雾对齐张量\(I\in\mathbb{R}^{P\times F\times H\times W}\)。应用于这些数组的大多数操作都是高度重复的：背景减法、局部滤波、梯度评估、几何重映射、角度分箱、阈值处理、二值清理和累积支持计算都在许多独立像素、箱或帧上重复。

这种结构在计算上非常关键。GPU 尤其适合处理由规则稠密数组运算、模板式邻域计算以及重复 map-reduce 风格 kernel 主导的工作负载，因为同一算术模式可以在大量数据元素上并行执行 \cite{NickollsBuckGarlandSkadron2008}。这里使用的单个算子本身都属于标准图像处理原语 \cite{GonzalezWoodsDIP,Szeliski2010Vision}；真正的工程问题在于，一旦把它们施加到 TB 级 12 bit 视频归档上，如果不组织成批量数据并行计算，其实际成本就会主导整个工作流。

因此，本论文中使用GPU加速不仅仅是实现偏好。它是方法设计的一部分。串行逐帧工作流会使简单的滤波和重映射步骤变得太慢和脆弱，无法进行迭代模型开发，而张量导向的CUDA流程将相同的处理链变成可以随着校准选择、目标定义和模型阶段演变而反复运行的东西。从这个意义上说，高吞吐量处理使工作流从实际上不可迭代变为实际上可迭代。

\subsection{删失、截断与稳定目标构造}

光学喷雾成像中的有限视场创造了统计问题和几何问题。当真实后期穿深超出可见窗口时，记录的轨迹不仅仅是噪声；它被观测限制右截断。在统计学中，这与更广泛的有限或截断因变量问题密切相关，其中观测值只是感兴趣潜在量的截断表示\cite{Tobin1958,BuckleyJames1979}。

图~\ref{fig:right-censoring-example-zh} 给出了当前工作流中的一个代表性喷雾图像实例。部分 plume 已经越过观测窗口上边界，只在边缘附近留下模糊的圆环状痕迹；另一些 plume 则仍完整位于可见区域内。这个例子说明，图像中表面上看到的“终止穿深”有时是由相机观测窗口决定的，而不是喷雾真实演化已经停止。

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{../images/right_censoring_example.png}
\caption{喷雾图像中的右截断实例。部分 plume 已越过观测窗口上边界，因此在上限附近仅留下模糊的圆环状边缘痕迹；另一些 plume 则尚未超过观测边界，仍完整显示在可见窗口内。}
\label{fig:right-censoring-example-zh}
\end{figure}

这种区别直接关系到代理模型训练。如果穿深轨迹达到测量边界并且可见终点被直接视为普通回归标签，学习目标在最需要长期穿深应该继续增加的区域向下偏置。在这种设置下，学习者被鼓励将图像消失解释为喷雾的物理饱和。实际上，这种标签构建可能在训练集混合截断和未截断轨迹时创建不稳定的后期梯度和人工平坦化。

因此，本论文中的降阶拟合阶段不仅作为平滑手段引入。它也是一个截断感知的目标构建步骤。拟合曲线提供轨迹的紧凑和更稳定表示，支持保守外推超出可见窗口，并减少下游学习对光学轨迹变得不完整的确切点的敏感性。这个角色在短预喷注入中尤其重要，因为起始注入和结束注入瞬态已经使原始信号形状复杂化\cite{ZhouLiYi2021,ZhouLiLaiWei2019}。

\subsection{多孔喷嘴 plume 配准与对称性利用}

多孔喷嘴引入了一个比通用喷雾分割更具体的问题。图像不包含一个孤立的喷雾，而是围绕共同喷嘴中心排列的几个喷雾，具有已知的多重性和近似重复的角度结构。同时，多孔行为不等同于单孔行为的任意重复，因为内部储罐流动、孔相互作用和喷雾间扰动可以改变结果喷雾形态\cite{JinKimKakamiNishidaOgataLuo2020,MopuriGrahnSedarskyHyvonen2024}。因此，计算问题不仅是检测喷雾像素，而是跨帧和工况一致地配准重复喷雾。

这就是为什么论文从笛卡尔图像坐标移动到喷嘴中心极坐标和喷雾对齐坐标。一旦喷雾在角度轴上描述，由喷嘴孔图案引起的重复结构就更容易分析，因为plume 占据可以总结为一维周期信号，而不是原始图像平面上的碎片边缘内容\cite{Szeliski2010Vision}。随后使用谐波分析不是任意的信号处理技巧，而是对称性利用的配准规则：已知的喷嘴多重性提供自然角度频率，其相位可用于旋转对齐\cite{CooleyTukey1965FFT}。

\subsection{喷雾成像中的度量定义与边界提取}

燃料成像文献还显示，喷雾度量的数值定义很重要。Magnotti和Genzale展示了不同的液体穿深度量可能导致实质性不同的结论，因为不同的可观测量对底层液体分布的不同方面做出响应\cite{MagnottiGenzale2013}。这直接与Mie 处理相关：报告的"穿深"值不是普适真值，而是应用于光学信号的测量规则的输出。

同样的谨慎适用于锥角和边界提取。Johnson等人表明，从背向散射柴油图像中基于阈值的锥角估计对所选强度阈值敏感，他们提出了基于轮廓的替代方法专门减少这种主观性\cite{JohnsonNaberLee2012}。这一观察从两个方面支持了本论文。首先，它证明了边界度量作为操作几何描述符而不是精确液体包络测量的合理性。其次，它激励使用累积分布穿深前沿和多个边界代理，因为稳健度量应依赖于喷雾的总体支持，而不是一个脆弱的领先像素或一个全局固定的阈值。

结果是一个不试图从强度"恢复物理"的工作流。相反，它使用图像处理理论和先前喷雾成像证据来构建可复现、物理可解释的几何可观测量，其局限性明确陈述。

\subsection{为什么确定性、异方差和教师引导学习阶段都必要}

确定性多层感知器是穿深的合理第一个代理，因为它可以将少量工况特征和时间映射到连续轨迹\cite{BishopPRML2006}。在存档基线笔记本中，网络使用物理缩放输入和与平滑性相关的惩罚来正则化轨迹形状。

不确定性感知扩展也是有动机的，因为不同的工况点和提取轨迹不具有相同的置信度。异方差高斯负对数似然是一种标准方法，让网络预测均值和输入依赖方差\cite{KendallGal2017}。在回归术语中，这对应于条件模型
\[
y=\mu(x,t)+\varepsilon(x,t),\qquad \varepsilon(x,t)\sim \mathcal{N}(0,\sigma^2(x,t)),
\]
这里应被理解为实用的统计假设，而不是声称喷雾物理在每个瞬间都是真正的高斯\cite{BishopPRML2006,KendallGal2017}。本论文的动机是，观察到的穿深扩散是许多未解决因素的聚合效应：喷嘴液压瞬态在预喷注入期间、储罐和针阀开启动力学在SOI附近、循环间传递动量的差异、同步偏移以及光学测量链的残余图像处理噪声。Zhou等人已经表明，短注入穿深行为在SOI附近强烈瞬态和阶段依赖\cite{ZhouLiYi2021}；在当前项目中，这些未建模的扰动不是逐一机械地建模，因此合理地将它们的组合效应视为输入依赖残差分布，其方差随工况和时间变化。在本论文中，这一想法被视为部分完成的扩展，而不是完成部署的模型，因为存档的第二阶段工件在其当前存储形式中不完整。

一旦监督在时间上变得不均匀，进一步的改进阶段就是有理由的。知识蒸馏被引入作为一种方式，通过软目标而不是one-hot或硬标签将教师网络的预测结构转移到学生\cite{HintonVinyalsDean2015}。当监督有限或不完整时，密切相关的教师-学生公式也被使用，因为教师可以在直接标签稀疏、噪声或缺失的区域提供稳定的信号\cite{TarvainenValpola2017}。在本论文中，这一逻辑被改编为部分观测穿深轨迹：在未截断的原始观测仍然可信的区域，它们应该直接细化学生；在晚期原始信号由于截断和样本丢失而变得不可靠的区域，教师延续比将消失的尾部视为硬物理标签更保守。这就是为什么第三学习阶段在方法论上不同于简单微调的原因：它结合了直接原始监督和教师指导的正则化，使模型可以在不被不完整的晚期观测向下拉拽的情况下提高早期时间保真度。

\section{研究材料与方法}



\subsection{工作流概览}

表~\ref{tab:workflow} 总结了本文实现的工作流以及各个项目工件的作用。

\begin{table}[t]
\centering
\begin{tabular}{|L{0.21\linewidth}|L{0.26\linewidth}|L{0.33\linewidth}|}
\hline
工件 & 主要作用 & 主要输出 \\
\hline
\codepath{GUI.py} & 人工喷嘴中心标定 & 某组视频的中心、半径和角向偏移 \\
\hline
\codepath{main.py} & 批量预处理与特征提取 & 按 plume 分解的穿深轨迹、二值几何指标和可选边界 CSV \\
\hline
\codepath{MLP/fit_raw_data.py} & 清洗、时延对齐与降阶拟合 & 清洗后的轨迹、标记轨迹、拟合参数以及拟合图 \\
\hline
\codepath{MLP/median_penetration_MSE.ipynb} & 确定性基线训练 & Stage-1 训练日志、scaler 状态、checkpoint 和 loss 曲线 \\
\hline
\codepath{MLP/median_penetration_NLL.ipynb} & 不确定性扩展 & Stage-2 训练日志、scaler 状态和归档的不确定性输出 \\
\hline
\codepath{MLP/toy_inference_from_run.ipynb} & 原型推理与可视化 & 均值/标准差预测曲线以及简单几何叠加图 \\
\hline
\end{tabular}
\caption{本文记录的已实现工作流。}
\label{tab:workflow}
\end{table}

\subsection{\texttt{GUI.py} 中的标定}

\texttt{GUI.py} 中的手动标定工作流包含三个界面阶段。首先，操作者在主标注界面中打开代表性帧，调整显示设置，并准备好用于后续批处理锚定的喷嘴中心视图。其次，启动专用标定对话框，用户在选定帧上点击喷嘴边缘点。第三，拟合得到的中心和半径回传到主界面，操作者可以结合 plume 导向线与圆环叠加效果检查几何恢复结果，随后保存配置供后续批量处理复用。

点选择对话框强制至少三次点击，并在输入所需点数后通过\texttt{calc\_circle}计算圆。拟合的圆提供喷嘴中心、校准半径和整个记录的角度偏移。

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/GUI_main_interface.png}
\caption{\texttt{GUI.py}主标注界面在校准前。操作员加载代表性帧，调整显示，并准备手动校准任务。}
\label{fig:gui-main-interface}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.78\linewidth]{../images/GUI_calibration_interface.png}
\caption{\texttt{GUI.py}中的校准点选择对话框。用户标记喷嘴边缘点，这些点稍后用于圆拟合和角度偏移估计。}
\label{fig:gui-calibration-interface}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/GUI_calibration_result.png}
\caption{圆拟合后的标定结果。恢复得到的喷嘴中心、导向方向和径向边界，为后续批量流程提供了统一的几何参考。}
\label{fig:gui-calibration-result}
\end{figure}

这种设计适合当前数据集，因为摄像机和喷嘴几何在记录内保持固定。一次校准并为所有帧重用相同中心可以避免下游喷雾角度分析中的帧间抖动。

\subsection{\texttt{main.py} 中的视频批处理}

\texttt{main.py} 中的文件处理器是图像分析工作流的中枢。对于每个 \texttt{.cine} 记录，\texttt{\_process\_cine\_file} 会把视频加载到当前数组后端，复用整个文件夹的环形掩码，调用 \texttt{mie\_multihole\_preprocessing} 与 \texttt{mie\_multihole\_postprocessing}，提取逐 plume 穿深曲线，计算基于边界的几何度量，并最终导出同步 CSV 与元数据文件。关键在于，这条链中数值开销最大的部分都尽量保留在后端本地：当 CuPy 可用时，卷积、角度分箱、重映射、阈值处理、连通域清理和形态学操作都会沿着 CUDA 路径执行，直到最终表格导出为止。

这一实现选择来自此前内部处理路线的实际限制。旧流程依赖逐帧 MATLAB 处理，缺少有效并行加速。这里批评的并不是 MATLAB 语言本身：例如 PIVlab 就是一个成熟的 MATLAB 开源 PIV 工具箱 \cite{ThielickeSonntag2021PIVlab}。真正的限制在于本地批处理架构；在这种形式下，即使处理约 50~GB 的视频也可能需要一天左右，并且仍然容易崩溃。因此，将主要图像操作转入批量 CUDA 导向流程，在本文中被视为方法设计的一部分，而不是单纯的软件优化。

\subsubsection{标定移交和批量锚定}

批量阶段从第3.2节中产生的校准开始。在\texttt{calc\_circle}中，点击的喷嘴边缘点\((x_i,y_i)\)用代数圆模型
\[
x_i^2+y_i^2 + D x_i + E y_i + F = 0,
\]
以最小二乘形式求解\texttt{np.linalg.lstsq}。恢复的参数是
\[
c_x=-\frac{D}{2}, \qquad c_y=-\frac{E}{2}, \qquad r=\sqrt{c_x^2+c_y^2-F},
\]
同一程序还从点击的边缘点相对于理想等距集估计平均角度偏移。\texttt{GUI.py}将这些值写入\texttt{config.json}，之后\texttt{main.py}可以处理整个文件夹而无需手动重新中心。这种一次校准是故意面向用户的，而不是完全自动的：操作员可以缩放、选择代表性帧，并用物理上有意义的喷嘴中心锚定整个批量\cite{Pratt1987CircleFit,Taubin1991CircleFit}。

\subsubsection{批量和鲁棒预处理}

批量流程中的第一个算法贡献是针对噪声Mie视频而不是理想二值图像设计的前景构建阶段。在\texttt{refined\_log\_subtraction}中，原始帧\(V_t(x,y)\)映射到
\[
L_t(x,y)=\log(V_t(x,y)+\varepsilon),
\qquad
L_{\mathrm{bkg}}(x,y)=\operatorname{median}_{t<t_{\mathrm{SOI}}} L_t(x,y),
\]
然后是对数比重建
\[
R_t(x,y)=\exp\!\left(L_t(x,y)-L_{\mathrm{bkg}}(x,y)\right)-1
=\frac{V_t(x,y)-B(x,y)}{B(x,y)}.
\]
然后实现将\(R_t\)乘以强度门\(M_t(x,y)=\mathbf{1}[V_t(x,y)>\alpha B(x,y)]\)，减去预注入基线，应用软阈值
\[
F_t(x,y)=\max\!\left(M_t(x,y)R_t(x,y)-R_0(x,y)-\tau,\,0\right),
\]
并按稳健分位数重新缩放结果，然后进行任何几何处理。这个序列对Mie视频很重要，因为原始强度不是液体体积分数的直接度量，而且实际目标是在非均匀照明、眩光和背景纹理下稳定几何提取，而不是精确的光学反演\cite{BohrenHuffman2004,Linne2013SprayImaging,GonzalezWoodsDIP}。

这种鲁棒预处理的需要在可视化级别上已经可以看到。图~\ref{fig:gui-gain-halo}比较了同一喷雾帧在两个显示增益下的情况，其余GUI参数保持不变。在较低增益下，喷雾核心相对紧凑。在较高增益下，明亮的光环在喷雾核心周围扩散并污染图像的更大区域。对于本论文的目的，这种行为被视为实用证据，表明基于Mie的强度强烈受多重散射、眩光和显示敏感背景污染的影响。因此，从单个灰度背景减法或直接强度阈值处理将对增益依赖性扩散过于敏感。这就是为什么实现的流程使用来自预注入统计的鲁棒背景估计，然后将表示转换为边缘主导的Sobel特征，其几何响应比原始亮度幅度更稳定\cite{BohrenHuffman2004,Linne2013SprayImaging,GonzalezWoodsDIP}。

\begin{figure}[t]
\centering
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{../images/GUI_gain_1.png}
\end{minipage}
\hfill
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{../images/GUI_gain_7.png}
\end{minipage}
\caption{在\texttt{GUI.py}中显示的同一喷雾帧在增益值1.0和7.0下的情况，其余显示参数保持不变。高增益视图使喷雾周围的明亮光环更加明显，说明多重散射、眩光和扩散背景污染如何扭曲基于原始强度的解释。}
\label{fig:gui-gain-halo}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/fig_mie_preproc_background.png}
\caption{从存储库工作流复制的中间预处理可视化。从左到右：原始帧、来自预注入统计的背景估计和对数减法前景。}
\label{fig:mie-preproc-background}
\end{figure}

\subsubsection{CUDA后端上的超高效Sobel高通滤波}

第二个贡献是直接应用于整个视频立方体的显式边缘增强高通阶段。在\texttt{arr\_3d\_sobel\_magnitude\_cupy}中，Sobel核\(K_x\)和\(K_y\)生成一次并使用\texttt{convolution\_2D\_cupy}应用：
\[
S_t(x,y)=\sqrt{\left(K_x * F_t\right)^2+\left(K_y * F_t\right)^2}.
\]
然后再次进行稳健缩放并用环形几何掩码。在实现术语中这很重要，因为它将低对比度灰度喷雾转换为边缘主导表示，同时保持整个操作在CuPy后端。贡献不是Sobel算子本身，这是经典的，而是将其集成到避免重复主机-设备复制的批量视频工作流中，为每个喷雾准备相同的下游几何处理\cite{GonzalezWoodsDIP,Szeliski2010Vision}。

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/fig_mie_preproc_sobel.png}
\caption{从存储库工作流生成的预处理输出显示前景图像、Sobel幅度和多孔分割阶段使用的最终稳健缩放高通表示。}
\label{fig:mie-preproc-sobel}
\end{figure}

\subsubsection{角度密度作为极坐标重采样}

预处理后，流程不会在笛卡尔坐标中通过轮廓追踪搜索喷雾方向。相反，\texttt{angle\_signal\_density\_auto}在校准喷嘴中心周围执行极坐标重参数化。对于每个像素，
\[
\theta(x,y)=\operatorname{atan2}(y-c_y,\;x-c_x)\bmod 360^\circ,
\]
每帧角度信号对于箱\(k\)的累积为
\[
A_t(k)=\sum_{(x,y)\in \mathcal{B}_k} I_t(x,y), \qquad
D_t(k)=\frac{A_t(k)}{\#\mathcal{B}_k},
\]
其中\(\mathcal{B}_k\)是角度落入箱\(k\)的像素集合。在CuPy实现中，分箱和加权和直接在GPU数组上通过\texttt{cp.digitize}和\texttt{cp.bincount}执行。概念上，这是将2D图像径向坍缩为1D角度占用信号，这比原始帧中的边缘片段更稳定，用于多孔对齐\cite{Szeliski2010Vision}。

\subsubsection{基于FFT的自适应旋转对齐}

然后从角度信号的周期性估计plume 导向角度，而不是从固定偏移。设
\[
\bar{A}(k)=\sum_t A_t(k)
\]
为时间求和的角度配置文件，设\(P\)为已知喷雾数量。函数\texttt{estimate\_offset\_from\_fft}计算\(\bar{A}\)的实FFT，读取喷雾谐波\(P\)的相位，并将其转换为角度偏移
\[
\hat{\phi}_0
=
-\frac{\arg\!\left(\mathcal{F}\{\bar{A}\}[P]\right)}{P}\frac{180^\circ}{\pi}.
\]
这就是使对齐自适应的原因：代码使用当前数据集的观测谐波相位而不是为每个记录手动输入参考角度。\texttt{main.py}然后构建喷雾方向
\[
\phi_p=\frac{360^\circ p}{P}-\hat{\phi}_0, \qquad p=0,\dots,P-1,
\]
并结合三角阈值占用掩码和短间隙填充来拒绝空角度扇区。FFT本身是标准的；这里的贡献是使用已知喷嘴多重性将角度密度信号转换为自动喷雾配准规则\cite{CooleyTukey1965FFT,Szeliski2010Vision}。

同一个 occupied-angle mask 还可以给出一个简单的 cone-angle proxy。具体做法是将 bin-wise 掩码视为定义在\([0,360^\circ)\)上的周期信号，把跨越\(0^\circ/360^\circ\)边界的 \texttt{True} 段合并为同一连续区间，再把每个连续区间的长度从 bins 换算为角度。得到的各段角宽描述了整段视频中每个 plume 实际占据的角度支持范围。在 notebook 的作图流程中，我们同时报告总 occupied angle 与各连续段角宽的平均值；后者可作为一个粗略的 cone-angle proxy，因为它在不需要逐帧重新做边缘拟合的情况下，概括了单个 plume 的平均角向展开宽度。

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/fig_angular_occupancy.png}
\caption{从存储库工作流生成的处理角度密度输出：每帧角度配置文件、时间求和配置文件和plume 导向角度、FFT谐波内容以及用于稳定多孔对齐的占用角度掩码。}
\label{fig:angular-occupancy}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/fig_angular_mask_comparison.png}
\caption{整段视频中同一组喷雾占据角度在两种坐标系下的表示。左图为依据喷嘴中心将占据角度 bins 投回图像空间后得到的旧式笛卡尔 mask，并附带总 occupied angle 与基于连续段平均宽度的 cone-angle proxy；右图为当前工作流直接使用的极坐标 occupied-angle mask，并标注每个连续 occupied segment 的角宽。}
\label{fig:angular-mask-comparison}
\end{figure}

\subsubsection{逆仿射映射和喷雾条带提取}

一旦知道目标角度，每个喷雾都重新映射到共同的喷嘴左条带坐标系统。在\texttt{build\_rotation\_affine}中构建的前向仿射变换是
\[
\begin{bmatrix}
u\\v
\end{bmatrix}
=
\mathbf{A}
\begin{bmatrix}
x\\y
\end{bmatrix}
+\mathbf{b},
\qquad
\mathbf{A}
=
\begin{bmatrix}
\cos\phi_p & \sin\phi_p\\
-\sin\phi_p & \cos\phi_p
\end{bmatrix},
\qquad
\mathbf{b}=\mathbf{t}-\mathbf{A}\mathbf{c},
\]
其中\(\mathbf{c}=(c_x,c_y)^\top\)是喷嘴中心，\(\mathbf{t}\)将喷嘴放置在输出条带的左边缘和中间高度。实际重映射使用逆关系
\[
\begin{bmatrix}
x_{\mathrm{src}}\\y_{\mathrm{src}}
\end{bmatrix}
=
\mathbf{A}^{-1}
\left(
\begin{bmatrix}
u\\v
\end{bmatrix}
-\mathbf{b}
\right),
\]
在\texttt{build\_affine\_inverse\_maps\_cupy}中实现。对于每个喷雾，映射数组\texttt{mapx}和\texttt{mapy}一次性预计算，\texttt{remap\_video\_cupy}使用最近、双线性、双三次或Lanczos核采样整个视频。需要强调的是，这两张图并不是喷雾亮度图：\texttt{mapx}中的像素值表示该输出条带像素在原始图像中采样的\(x\)坐标，\texttt{mapy}中的像素值表示对应的原始图像\(y\)坐标。多孔流程使用固定条带大小\texttt{OUT\_SHAPE = (outer\_radius/2, outer\_radius)}并应用三角喷雾掩码。这种逆映射公式计算效率高且数值清洗，因为每个下游喷雾度量都在相同的局部坐标系中测量\cite{Szeliski2010Vision}。

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/fig_efficient_rotation.png}
\caption{从存储库工作流生成的逆仿射重映射阶段可视化。左上与右上分别为\texttt{mapx}和\texttt{mapy}，其像素亮度编码的是每个输出条带像素对应的原始图像\(x/y\)采样坐标，而不是喷雾亮度。左下为单个喷雾的旋转条带，图中标出\((H/2,\mathrm{inner\_radius})\)处的喷孔局部原点；轴向穿深曲线以\(\mathrm{inner\_radius}+\)metric的形式叠加，而极坐标二值穿深则画成以喷孔为圆心、半径为\(r\)的圆弧。右下为重映射后施加的三角喷雾掩码。}
\label{fig:efficient-rotation}
\end{figure}

\subsubsection{对齐喷雾条带的稳健CDF穿深}

穿深估计器故意基于累积强度支持而不是单个最亮像素或最外层孤立斑点。对于每个喷雾条带\(I_p(t,y,x)\)，\texttt{penetration\_cdf\_all\_plumes}首先坍缩跨流方向，
\[
H_p(t,x)=\sum_y I_p(t,y,x),
\]
从每帧减去左边缘基线，截断负值，并通过自动阈值、最大组件清理和二值开运算生成支持掩码。在该支持内，\texttt{penetration\_cdf\_front}形成轴向累积分布
\[
C_p(t,x)=\frac{\sum_{\xi \le x} \tilde{H}_p(t,\xi)}{\sum_{\xi} \tilde{H}_p(t,\xi)+\varepsilon},
\]
并定义穿深前沿为满足
\[
\hat{x}_p(t)=\min \{x : C_p(t,x)\ge q\},
\]
其中\(q\)接近1。然后通过\texttt{np.maximum.accumulate}强制单调性，从喷嘴环形减去内半径以表示距离，\texttt{main.py}中丢弃显示非物理后退跳跃或只有很短有效运行的轨迹。这个CDF前沿是工作流的主要实用贡献：它比朴素的最大索引定义对断裂的边缘像素不那么敏感，同时仍然产生与标准柴油喷雾使用对齐的穿深量\cite{Dent1971Penetration,HiroyasuArai1990,NaberSiebers1996}。

\subsubsection{边界度量和几何代理}

二值喷雾条带也传递给\texttt{extract\_single\_plume\_features}，该程序计算几个次要可观测量。投影面积是
\[
A_p(t)=\sum_{x,y} B_p(t,y,x).
\]
轴向二值穿深从占用超过像素阈值的最右列获得，而极穿深从上边界点云和下边界点云定义为
\[
R_p(t)=\max\!\left(
\max_i \sqrt{x_{u,i}^2+y_{u,i}^2},
\max_j \sqrt{x_{l,j}^2+y_{l,j}^2}
\right).
\]
代码还存储一个类似体积的几何代理，
\[
V_p(t)=x_{\mathrm{scale}} \frac{\pi}{4}\sum_x \left(w_u(t,x)+w_l(t,x)\right)^2,
\]
其中\(w_u\)和\(w_l\)是上半宽和下半宽，\(x_{\mathrm{scale}}=1/\cos((180^\circ-\theta_{\mathrm{umb}})/2)\)在需要时补偿伞角倾斜。锥角代理通过两种方式计算：通过平均局部边界角度
\[
\alpha_{\mathrm{avg}}(t)=\overline{\arctan(y_u/x_u)}-\overline{\arctan(y_l/x_l)},
\]
和通过拟合固定截距边界线并取
\[
\alpha_{\mathrm{lg}}(t)=\arctan(m_u)-\arctan(m_l).
\]
最后从靠近喷嘴的检测窗口推断近喷嘴开启和关闭时间。这些度量很重要，因为它们将每个对齐条带转换为紧凑的几何状态向量；然而，本论文中的下游建模仍然专注于穿深，这是存档笔记本中最支持的目标\cite{Linne2013SprayImaging,LefebvreMcDonell2017}。

\begin{figure}[t]
\centering
\includegraphics[width=0.98\linewidth]{../images/fig_mie_metrics_summary.png}
\caption{在喷雾对齐和二值化后生成的定量输出示例：穿深描述符、喷雾大小、锥角代理和代表性的二值喷雾条带最大投影。}
\label{fig:mie-metrics-summary}
\end{figure}

\subsection{\codepath{MLP/fit_raw_data.py} 中的轨迹清洗与降阶拟合}

学习模型不直接在原始导出轨迹上训练。相反，\texttt{fit\_raw\_data.py}首先对它们进行清洗与时间对齐。对每个 plume，该脚本先定位最早的有效穿深段，取起始后的前几个非零样本点（当前由 \texttt{NUM\_POINTS\_SOI\_LINEAR\_REGRESSION} 控制，默认是 2 个），对这些早期正值点做线性回归，并在斜率为正时用回归直线与 \(x\) 轴的截距作为亚帧级开启延迟估计。如果 area 序列提供了更早的起始标记，则扫描从该帧锚定；否则代码退回到首个正穿深帧。得到的原始延迟随后会在同一组 plume 内被裁剪到一个以中位数为中心的小窗口中，以避免单条异常 plume 把整组过度平移。

该延迟估计随后以两种耦合方式进入对齐过程。穿深数组本身按最接近的整数帧数整体左移，而对齐后的时间轴则继续保留减去亚帧残差后的精确偏移。因此，这里的实现不只是简单丢弃前导零，而是在后续降阶拟合之前执行一次保留亚帧时序信息的 plume 起始配准。完成对齐后，脚本再移除突然不现实的跳跃，将像素转换为校正的物理长度，并通过一组基于规则的掩码标记低质量拟合。

每个接受的轨迹随后用平滑的双分支模型拟合，
\[
S(t)=\left(1-w(t)\right)k_{\mathrm{sqrt}}\sqrt{t}
+w(t)k_{\mathrm{quarter}}t^{1/4},
\qquad
w(t)=\sigma\!\left(\frac{t-t_0}{s}\right),
\]
其中\(k_{\mathrm{sqrt}}\)、\(k_{\mathrm{quarter}}\)、\(t_0\)和\(s\)在对数空间中优化。具体来说，拟合问题表述为稳健的非线性最小二乘估计，
\[
\min_{\theta}
\sum_i \rho_\delta\!\left(S(t_i;\theta)-y_i\right),
\qquad
\theta=
\left[\log k_{\mathrm{sqrt}},\log k_{\mathrm{quarter}},\log t_0,\log s\right],
\]
使用Huber惩罚
\[
\rho_\delta(r)=
\begin{cases}
\tfrac{1}{2}r^2, & |r|\le \delta,\\
\delta\left(|r|-\tfrac{1}{2}\delta\right), & |r|>\delta.
\end{cases}
\]
在实现中，这个目标通过\texttt{scipy.optimize.least\_squares(..., method="trf", loss="huber")}求解，其中\(\delta=1\)，而对数参数化保持拟合增益和转换尺度为正\cite{Huber1964,NocedalWright2006}。这个拟合不是作为无根据的便利曲线引入的。它是柴油喷雾穿深文献中使用的分阶段变化观点的降阶、数据驱动适应：Hiroyasu和Arai描述喷雾发展在不同阶段的穿深行为变化\cite{HiroyasuArai1990}，后来关于起始注入瞬态的工作表明，短注入很难用一个刚性经验表达式在整个轨迹上表示\cite{ZhouLiYi2021}。这个考虑在本论文中尤其重要，因为当前实验使用预喷柴油注入，其近SOI部分短、高度瞬态且相对噪声。

因此，该模型应被理解为时间参数化而不是显式物理定律。其目的是双重的：提供在SOI之外的可信外推，下游代理需要稳定目标；以及在SOI附近提供可接受的插值，其中相对误差较大，图像轨迹单独难以约束早期时间物理。Sigmoid门对于该角色很重要。它不是在两个经验分支之间施加硬断点，而是在两个分支之间创建可微过渡，这保持了拟合曲线的平滑性，使其更兼容后续神经网络阶段。换句话说，早期瞬态物理没有被强制进入刚性封闭形式定律；它们只是被足够好地近似，以便后续网络可以从完整的工况点输入中学习剩余的条件依赖行为。

当将拟合与喷雾文献中的扩展EOI表达式进行比较时，这种解释变得更加清晰。Zhou等人报告的EOI后模型区分了全局注入时钟\(t\)和延迟时钟\((t-t_i)\)，因为后期穿深分支只有在结束注入动力学传播到下游后才激活\cite{ZhouLiLaiWei2019}。当前管线对这一延迟采用两层处理。首先，预处理阶段利用上文所述的早期线性外推，为每条 plume 估计一个起始延迟，并把观测序列平移到共同的 \(t\approx 0\) 参考上。其次，降阶拟合本身仍然没有把延迟时钟显式写进参数化分支，而是使用
\[
w(t)=\sigma\!\left(\frac{t-t_0}{s}\right)
\]
在学习的转换时间\(t_0\)之前抑制四分之一根分支，然后允许\(t^{1/4}\)项在该转换后主导。这在代数上不等同于用\(t^{1/4}\)替换\((t-t_i)^{1/4}\)。而是工程重新参数化：在有限观测窗口中，延迟第四根分支通常可以用一个门控未移位第四根分支很好地表示，调整幅度和转换位置，特别是当该分支的早期部分通过\(1-w(t)\)或\(w(t)\)在零附近被强烈衰减时。从这个意义上说，延迟物理时钟被吸收进学习的切换参数\((t_0,s)\)和分支幅度中，而不是作为固定已知常数强加。

因此，我认为这种工程方法对于本论文所述目标是合理的，有一个明确的边界。它是合理的，因为拟合用作噪声轨迹的平滑压缩器，而不是最终的机械喷雾定律；它保持曲线可微以供后续学习；并将工况依赖性留给神经网络而不是将每个物理效应强加于封闭形式拟合。边界是，这种解释应被呈现为基于经验穿深理论的降阶代理，而不是证明移除的时钟物理消失。更严格的物理导向研究将直接将当前拟合与显式在\((t-t_i)\)中书写的分支进行比较并报告消融。

存档的本地分析支持这种解释。\texttt{MLP/interpretatable\_test} 中的探索笔记本明确把该模型描述为受 Hiroyasu--Arai 启发的双分支混合，而 \texttt{fitted\_stats\_analysis.ipynb} 以及归档的 clean-fit CSV 表格则表明，\(k_{\mathrm{sqrt}}\) 更像是一个可选的形状描述符，而不是稳定一致的物理系数。在当前 clean-fit 归档中，拟合转换时刻处的平方根分支幅值，在大多数被接受样本中都小于四分之一根分支的一半，因此很多成功拟合实际上会退化为以四分之一根为主导的曲线。对本文的预期用途而言，这一点是可以接受的：物理驱动量会在后续直接输入神经网络，而拟合本身则保持自适应且物理负担较轻。在 \texttt{MLP/synthetic\_data} 下的归档生产输出中，123,902 个候选行中有 105,594 个被原始非线性拟合标记为成功，对应于约 \(85\%\) 的实用成功率，而且这还是在更严格的后拟合掩码阶段之前。该拟合的实际目的，并不是宣称找到一条普适的穿深定律，而是把带噪 plume 轨迹压缩为平滑、可微且适合训练的目标，同时保留数据集中的异方差变化供后续学习阶段使用。

更严格的后拟合掩码阶段也是工程贡献的重要部分。在\texttt{apply\_filter\_masking}中，脚本首先在\(t_{\mathrm{far}}=\SI{5}{ms}\)处评估拟合曲线形成远时间穿深代理。只有当该远时间值保持在广泛的物理上合理窗口内并且拟合参数通过基本的合理性检查\(t_0\)、\(s\)、点数和拟合成本时，才保留该行。然后代码使用中位绝对偏差计算稳健的分组异常值分数，
\[
z_{\mathrm{robust}}(x)=0.6745\,\frac{x-\operatorname{median}(x)}{\operatorname{MAD}(x)+\epsilon},
\qquad
\operatorname{MAD}(x)=\operatorname{median}\!\left(\lvert x-\operatorname{median}(x)\rvert\right),
\]
应用于\(t_0\)、\(\log(1+\mathrm{RMSE})\)和\(\log(1+\mathrm{cost\_per\_point})\)在每个文件组内。在任何诊断上\(z_{\mathrm{robust}}\)超过3的行被标记为异常值并从清洗档案中排除。这在后续神经网络阶段很重要，因为它在扭曲训练分布之前移除了显然不稳定的拟合。结果是一个故意保守的策划步骤：网络不会看到每个拟合行，而只会看到在远时间保持数值合理的轨迹。

\subsection{v1 直接特征 MLP 中的稀疏特征不稳定性}

在当前 v2 架构确定之前，一个较早的九特征 MLP 曾以直接输入压力和喷嘴直径的方式进行训练。归档于 \codepath{MLP/v1\_direct\_feature\_training/slides\_sparse\_feature\_instability} 的系统性诊断研究揭示了一个结构性问题。当前数据集中，喷嘴直径仅有六个观测层级，且并非在所有压力组合下均有覆盖：全部六个直径层级仅出现于狭窄工况族 $(P_{\mathrm{inj}}=\SI{2000}{bar},\;P_{\mathrm{cb}}=4,\;P_{\mathrm{ch}}\in\{5,10,15\}\,\si{bar})$。喷射压力也只有四个层级。由于光滑 MLP 把这些输入视为连续变量，它会在直径轴和压力轴上产生数据无法约束的导数结构。对直径做密集扫描时出现五到六次梯度符号变化，最差截线偏离比低于一，曲率范数高达 $10^7$ 量级；喷射压力在稀疏支持切片上同样表现出不受数据约束的外推行为。这一病理是稀疏特征几何的结构性后果，在时间维度上持续存在，而非训练噪声的偶发产物。加大正则化只能以牺牲全局容量为代价来抑制不稳定性；根本问题在于光滑 MLP 被要求从一个本质上离散的采样格上学习具有物理意义的连续规律。v2 架构因此从源头上解决了这一问题，直接移除了这些稀疏输入。

\subsection{\codepath{MLP/v2\_engineered\_feature} 中的工程化特征设计}

v2 代理模型将四个弱支持的压力和直径输入替换为一个来自 Hiroyasu--Arai 破碎后分支的硬物理缩放群 \cite{HiroyasuArai1990}。从破碎后形式
\[
S(t)\;\propto\;K_p\!\left(\frac{\Delta P}{\rho_a}\right)^{1/4}d_n^{1/2}\,t^{1/2},
\]
对喷射压力、气缸压力、气缸密度与喷嘴直径的全部依赖坍缩为一个逐工况的长度尺度
\[
A\;=\;\left(\frac{\Delta P}{\rho_a}\right)^{1/4}d_n^{1/2},
\]
该量在 \texttt{engineered\_feature\_common.build\_canonical\_feature\_table} 中由测试矩阵 JSON 计算得到。对于 Nozzle1--Nozzle8，气缸密度直接取自 \texttt{chamber\_density\_kg\_per\_m3}；对于只记录 \texttt{chamber\_pressure\_bar} 的 DS300，则按对 Nozzle1--8 显式条目标定的线性律 $\rho_a=\SI{5}{kg/m^3}\times P_{\mathrm{ch}}/\SI{4.42}{bar}$ 反推 $\rho_a$。

四个稀疏列 \texttt{diameter\_mm\_z}、\texttt{log\_injection\_pressure\_bar\_z}、\texttt{log\_chamber\_pressure\_bar\_z} 与 \texttt{log\_delta\_pressure\_bar\_z} 被完全移除，保留两个特征向量变体。五特征 \texttt{a\_only}
\[
[\,t_{\mathrm{norm}},\;\theta_{\mathrm{tilt},z},\;n_{\mathrm{plumes},z},\;t_{i,z},\;P_{\mathrm{cb},z}\,]
\]
中 $A$ 仅作为目标除数，不进入输入；六特征 \texttt{a\_plus\_log\_a} 在此基础上额外加入 $\log A_z$ 作为一维残差轴。训练目标被重参数化为无量纲穿深 $\hat{S}(t)=S(t)/A$，网络预测 $\hat{\mu}$ 与 $\log\hat{\sigma}^2$，物理均值与标准差在推理时通过 $\mu_S=A\,\hat{\mu}$ 与 $\sigma_S=A\,\hat{\sigma}$ 精确还原。

在任何优化之前，\texttt{run\_pretrain\_collapse\_check} 在实际数据集上检验缩放假设：它将每条代表性轨迹除以自身的 $A$，报告逐时间切片方差缩减情况，并计算 $\hat{S}(t)$ 对保留特征的线性回归 $R^2$。在归档 run 中，该检验通过，中位数 \SI{0.5}{ms} 后方差缩减率为 59.5\%（坍缩比 0.405），稀疏特征 $R^2$ 从物理目标上的 0.34 降至缩放目标上的 0.14，证实 $A$ 已经解释了主要的直径与压力依赖性，网络只需学习残余的、由稠密轴主导的变化结构。若检验失败，训练默认中止，使得物理先验在实际数据集上可证伪而非默认成立。

驱动脚本 \texttt{train\_stage1\_mse.py} 与 \texttt{train\_stage2\_nll.py} 还引入了组感知数据划分：\texttt{assign\_splits\_by\_group} 按整组 \texttt{sample\_group\_id} 分配训练、验证、测试集，防止共享同一工况点的曲线之间发生泄漏。Stage~2 严格从 Stage-1 工程化 run 目录热启动并复用其 scaler 状态，保证两个阶段的变体选择与 $A$ 缩放完全一致。

\subsection{无量纲目标上的 Stage-1 MSE 基线}

Stage~1 在代表性轨迹子集上训练确定性均值模型。对于每个文件级工况组，在 $t_{\mathrm{far}}=\SI{5}{ms}$ 处评估拟合曲线，计算该时刻的组中位穿深，并保留最接近该中位的行作为 Stage-1 代表性轨迹。在归档 run \codepath{stage1\_engineered\_mse\_a\_only\_20260410\_020317} 中，该过程产出 526 条代表性轨迹，按组划分为训练、验证与测试集。

作用于无量纲目标 $\hat{S}$ 的损失为
\[
\begin{aligned}
\mathcal{L}_{\mathrm{stage1}}
=\;&
\operatorname{MSE}\!\left(\hat{\mu},\hat{S}\right)
+\lambda_{\mathrm{var}}
\mathbb{E}\!\left[\left(\log \hat{\sigma}^2-c_0\right)^2\right]\\
&+\lambda_{1}
\mathbb{E}\!\left[\operatorname{ReLU}\!\left(-\frac{\partial \mu_S}{\partial t}\right)^2\right]
+\lambda_{2}
\mathbb{E}\!\left[g(t)\operatorname{ReLU}\!\left(\frac{\partial^2 \mu_S}{\partial t^2}\right)^2\right],
\end{aligned}
\]
其中 $c_0=-2$ 为对数方差先验中心，$g(t)=\sigma((t-t_c)/\tau_c)$ 将凹性惩罚门控至瞬态区域之后，导数惩罚在物理重建 $\mu_S=A\,\hat{\mu}$ 上计算，以确保单调性与凹性约束在可观测尺度上生效。网络为三隐层 MLP，维度 $[512,512,128]$，GELU 激活，dropout~0.3，使用 AdamW 优化器与梯度裁剪及早停。该 run 在第 115 个 epoch 达到最佳验证 MSE（缩放目标）18.94，测试集物理 MAE 为 \SI{8.1}{mm}。run 目录包含 \codepath{best\_model\_stage1.pt}、\codepath{scaler\_state.json}、epoch 与迭代损失日志以及 \codepath{pretrain\_collapse\_summary.png}。

\subsection{Stage-2 异方差 NLL 扩展}

Stage~2 从 Stage-1 checkpoint 热启动，将训练集扩展至所有经过筛选的轨迹，并以缩放目标上的异方差高斯负对数似然替换 MSE 数据拟合项：
\[
\mathcal{L}_{\mathrm{stage2}}
=
\underbrace{\mathbb{E}\!\left[
\frac{1}{2}
\left(
\log \hat{\sigma}^2
+\frac{(\hat{S}-\hat{\mu})^2}{\hat{\sigma}^2}
\right)\right]}_{\text{缩放目标上的异方差 NLL}}
+\lambda_{1}
\mathbb{E}\!\left[\operatorname{ReLU}\!\left(-\frac{\partial \mu_S}{\partial t}\right)^2\right]
+\lambda_{2}
\mathbb{E}\!\left[g(t)\operatorname{ReLU}\!\left(\frac{\partial^2 \mu_S}{\partial t^2}\right)^2\right],
\quad\hat{\sigma}^2=\exp(\log\hat{\sigma}^2)+\varepsilon.
\]
导数惩罚仍然在物理空间重建 $\mu_S=A\,\hat{\mu}$ 上计算，特征映射与 Stage~1 完全相同，确保两个阶段的 $A$ 缩放与变体选择一致。行表扩展至 14,594 条经过筛选的轨迹，保留所有通过拟合级掩码以及组内远时刻 z-score 过滤的行。数据划分在 \texttt{sample\_group\_id} 层级完成，而不是在单条 plume 轨迹层级完成，因此同一物理工况下的轨迹不会同时泄漏到训练、验证和测试分区中。每条轨迹在 \SIrange{0}{5}{ms} 内采样 512 个时间点，因此归档的 Stage-2 表共包含 7,472,128 个逐点样本：训练集为 370 个组中的 10,333 条轨迹，验证集为 78 个组中的 2,092 条轨迹，测试集为 78 个组中的 2,169 条轨迹。

保留下来的轨迹覆盖了归档数据中的主要操作范围：控制背压从 1 到 \SI{4}{bar}，喷射压力从 1,400 到 \SI{2,200}{bar}，喷射持续时间从 380 到 \SI{1,200}{\micro\second}，拟合远时刻穿深从 \SI{58.1}{mm} 到 \SI{202.6}{mm}。因此 Stage~2 学到的并不是单一中位曲线，而是在异常值剔除后仍然存在的组内 plume 差异与重复实验差异。异方差输出用于把纯 MSE 目标会混在一起的两个量分开：条件平均穿深轨迹，以及围绕该平均轨迹的条件离散度。

训练沿用 Stage~1 的特征映射与 scaler 状态。输入向量仍为 \([\texttt{time\_norm\_0\_5ms},\theta_z,N_{\mathrm{plume},z},\Delta t_{\mathrm{inj},z},p_{\mathrm{back},z}]\)，网络仍为三隐层 MLP，维度为 $[512,512,128]$，并从 \codepath{best\_model\_stage1.pt} 热启动。Stage~2 run 使用 AdamW，学习率 \(8\times10^{-4}\)，权重衰减 \(10^{-4}\)，batch size 为 96，梯度裁剪范数为 1.0，对数方差边界为 \([-10,6]\)，早停耐心为 35。因此，该阶段在数据拟合项上的有意变化，是把确定性 MSE 替换为带学习方差输出的高斯 NLL。

\begin{table}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{|L{0.20\linewidth}|L{0.08\linewidth}|L{0.14\linewidth}|L{0.13\linewidth}|L{0.12\linewidth}|L{0.12\linewidth}|}
\hline
\codepath{epoch\_loss.csv} 记录 & Epoch & 逐点样本数 & Loss / 缩放 NLL & 物理 MAE & 平均预测标准差 \\
\hline
最佳验证 epoch 的训练集 & 66 & 5{,}290{,}496 & 2.156 / 2.148 & \SI{9.83}{mm} & \SI{12.27}{mm} \\
验证集，最佳 checkpoint & 66 & 1{,}071{,}104 & 2.038 / 2.038 & \SI{8.81}{mm} & \SI{11.71}{mm} \\
验证集，早停 epoch & 101 & 1{,}071{,}104 & 2.053 / 2.053 & \SI{8.97}{mm} & \SI{12.11}{mm} \\
测试集，恢复最佳 checkpoint & 101 & 1{,}110{,}528 & 2.083 / 2.083 & \SI{9.18}{mm} & \SI{11.44}{mm} \\
\hline
\end{tabular}
\caption{来自 \codepath{MLP/runs\_mlp/stage2\_engineered\_nll\_a\_only\_20260410\_020607/epoch\_loss.csv} 的 Stage-2 epoch 级损失摘要。测试行是在早停后写入的，但测试评估前模型权重已恢复至最佳验证 checkpoint。}
\label{tab:stage2-epoch-loss}
\end{table}

表~\ref{tab:stage2-epoch-loss}显示，最佳验证 NLL 出现在第 66 个 epoch，而训练在第 101 个 epoch 早停，因为后续没有获得更低的验证损失。留存测试集 MAE 为 \SI{9.18}{mm}，与验证集 MAE 接近；平均预测标准差为 \SI{11.44}{mm}，仍大于逐点误差尺度。这正是该阶段所需要的行为：模型不再被迫把所有 plume 间残差变化都压入均值函数，而是给出非零的条件不确定性，供后续碰撞概率估计与蒸馏阶段使用。run 目录包含 \codepath{best\_model\_stage2.pt}、\codepath{scaler\_state.json}、\codepath{row\_table.csv}、\codepath{epoch\_loss.csv}、\codepath{iteration\_loss.csv}、\codepath{test\_summary.csv} 和 \codepath{stage2\_loss\_curves.png}；其中 \codepath{best\_model\_stage2.pt} 作为后续蒸馏阶段的教师 checkpoint。

\subsection{原始 CDF 蒸馏与喷射起始精化}

NLL 阶段在拟合训练目标上产出了良好的异方差预测，但拟合曲线在整个时间轴上的忠实程度并不均匀。在注入起始附近，绝对穿深很小，任何时间偏移、拟合延迟模糊或平滑性先验都会产生相对较大的分数误差，因此每条预测曲线的最早部分仍然由拟合目标先验主导，而不是由直接观测到的原始羽流动力学决定。图~\ref{fig:stage2-toy-inference-early-time}以代表性玩具推理扫描展示了这一现象。

\begin{figure}[t]
\centering
\includegraphics[width=0.82\linewidth]{images/stage2_toy_inference_2000bar_5bar.png}
\caption{Stage-2 NLL 模型在代表性 \(\SI{2000}{bar}\)、\(\SI{5}{bar}\) 工况下的玩具推理结果。预测整体平滑且具有不确定性感知，但轨迹最早部分仍然主要由拟合目标先验塑造，这正是后续蒸馏精化的动机。}
\label{fig:stage2-toy-inference-early-time}
\end{figure}

为了在原始 CDF 观测上精化早期时间动态，同时防止在晚期稀疏区域崩塌，一个知识蒸馏阶段被实现于 \codepath{MLP/distillation\_plus\_raw\_series.py} 中，并归档于 \codepath{runs\_mlp/distill\_cdf\_onset\_v2\_20260410\_103413}。Stage-2 NLL 模型作为固定教师；学生网络继承教师权重，并通过原始 CDF 与教师指导的联合训练进一步精化。

\textbf{CDF 覆盖率区制。}  由于原始 CDF 序列存在右截断问题（到 $t\approx\SI{3}{ms}$ 时，许多工况已损失超过 70\% 的测量值），无法在时间轴上均匀地施加直接的原始监督。对于每个工况组，原始观测被分入 $[0,5]\,\si{ms}$ 上 $\Delta t=\SI{0.1}{ms}$ 的时间箱，每箱的平滑样本数作为覆盖率代理，确定三个互斥区制：\texttt{raw\_reliable}（平滑覆盖率 $\geq$ 峰值的 70\%）、\texttt{raw\_uncertain}（覆盖率在 20\%--70\% 之间）、\texttt{teacher\_only}（覆盖率低于 20\% 或每箱少于 4 个样本）。区制转换需要至少两个连续时间箱，以防止孤立下跌触发过早的区制切换。

\textbf{监督权重。}  每个网格点接收两个独立的权重通道：

\begin{center}
\begin{tabular}{lcc}
\toprule
区制 & 原始权重 $w_{\mathrm{raw}}$ & 知识蒸馏权重 $w_{\mathrm{kd}}$ \\
\midrule
\texttt{raw\_reliable}  & 1.0 & 0.25 \\
\texttt{raw\_uncertain} & 0.0 & 1.0  \\
\texttt{teacher\_only}  & 0.0 & 0.75 \\
\bottomrule
\end{tabular}
\end{center}

两个通道还通过全局时间箱再平衡权重（逆频率，裁剪至 $[0.5,2.0]$）进一步调制。在 \texttt{raw\_reliable} 箱中，原始 NLL 为主要信号，辅以轻度 KD 正则化；在 \texttt{raw\_uncertain} 箱中，原始监督被丢弃，教师 KL 散度占主导；在 \texttt{teacher\_only} 箱中，KD 权重略有降低，以反映教师在外推区域的不确定性。

\textbf{复合损失函数。}  训练目标为
\[
\mathcal{L} = \mathcal{L}_{\mathrm{raw}} + \mathcal{L}_{\mathrm{KD}} + \lambda_{\mathrm{onset}}\,\mathcal{L}_{\mathrm{onset}} + \lambda_{\mathrm{anchor}}\,\mathcal{L}_{\mathrm{anchor}} + \lambda_{d_1}\,P_{d_1} + \lambda_{d_2}\,P_{d_2},
\]
其中 $\mathcal{L}_{\mathrm{raw}}$ 为物理空间 $(\mu,\sigma^2)$ 相对于原始 CDF 目标的高斯 NLL，由 $w_{\mathrm{raw}}$ 加权；$\mathcal{L}_{\mathrm{KD}}$ 为学生到教师的前向 KL 散度 $\mathrm{KL}(q_{\mathrm{student}}\|p_{\mathrm{teacher}})$（物理空间），由 $w_{\mathrm{kd}}$ 及教师置信因子
\[
w_{\mathrm{conf}}(t) = \mathrm{clamp}\!\left(\frac{\sigma_{\mathrm{ref}}}{\sigma_{\mathrm{teacher}}(t)},\;0.25,\;1.0\right),\quad\sigma_{\mathrm{ref}}=\SI{10}{mm}
\]
共同加权；$\mathcal{L}_{\mathrm{onset}}$ 为新增起始 logit 输出相对于斜坡目标 $\min(t/t_{\mathrm{ramp}},1)$ 的 BCE 损失，仅在 $t\leq\SI{0.2}{ms}$ 时激活（$\lambda_{\mathrm{onset}}=0.1$）；$\mathcal{L}_{\mathrm{anchor}}$ 为将 $\mu(t\approx0)$ 拉向零的二次惩罚，在前 \SI{0.15}{ms} 内逐渐消退（$\lambda_{\mathrm{anchor}}=0.01$）；$P_{d_1}=\operatorname{ReLU}(-\Delta\mu)^2$ 强制单调性；$P_{d_2}=\operatorname{ReLU}(+\Delta^2\mu)^2$ 在 $t\geq\SI{0.5}{ms}$ 后强制凹性。

\textbf{学生模型架构。}  学生为三输出 MLP，在教师的两个输出 $[\hat\mu,\;\log\hat\sigma^2]$ 基础上增加一个起始 logit。所有共享层从教师 checkpoint 复制；起始头的最终线性层初始化为零（sigmoid $\to0.5$）。所有损失均在物理空间计算，学生和教师输出在评估 $\mathcal{L}_{\mathrm{KD}}$、$\mathcal{L}_{\mathrm{raw}}$ 及形状先验之前乘以每样本的 $A$。

\textbf{训练与结果。}  学生以 AdamW（$\mathrm{lr}=3\times10^{-3}$）优化，梯度裁剪范数为~1.0，早停耐心为~20。在归档 run \codepath{distill\_cdf\_onset\_v2\_20260410\_103413} 中，最佳验证损失为第 55 个 epoch 的~2.327（验证原始 NLL~2.256，验证 KD KL~0.018，验证起始 BCE~0.308）。在留存测试集上：原始 NLL~2.29，KD KL~0.019，起始 BCE~0.309，导数惩罚可忽略（$P_{d_1}\approx6.5\times10^{-7}$，$P_{d_2}\approx2.3\times10^{-5}$）。run 目录包含 \codepath{best\_model\_refinement.pt} 和 \codepath{loss\_curves.png}；图~\ref{fig:distillation-loss} 展示了训练曲线。

\begin{figure}[t]
\centering
\includegraphics[width=0.92\linewidth]{images/distillation_loss_curves.png}
\caption{来自 \codepath{runs\_mlp/distill\_cdf\_onset\_v2\_20260410\_103413/loss\_curves.png} 的蒸馏阶段训练曲线。复合损失收敛至第 55 个 epoch 的最佳验证值~2.327。}
\label{fig:distillation-loss}
\end{figure}

\subsection{原型推理 notebook}

笔记本\codepath{MLP/toy_inference_from_run.ipynb}演示了如何加载归档的 run 目录进行手动推理。存档输出显示，当指向上述第二阶段运行目录时，原型识别出的可用 checkpoint 为\codepath{best_model_stage1.pt}。然后生成均值和标准差曲线，并在简单几何掩码上叠加。

该原型作为概念证明是有用的，因为它练习了缩放状态、训练网络接口和简单的下游可视化路径。它不在此用作完成碰撞检测产品的证据。

\section{结果}



\subsection{已提取与清洗的轨迹资产}

当前工作区在 \texttt{MLP/synthetic\_data} 下包含 10 个 campaign 文件夹，并在对应的 \texttt{clean} 子文件夹中保存了 295 个 clean fitted CSV 文件。这说明图像处理工作流并非停留在草稿阶段，而是已经从多个 campaign 文件夹中生成了大量面向穿深建模的清洗资产。

图~\ref{fig:clean-fit-example} 展示了归档输出目录中的代表性清洗后穿深图。实线曲线表示清洗后的轨迹，虚线曲线表示由 \texttt{cdf} 路径得到的降阶拟合曲线。该图清楚说明了 \texttt{MLP/fit\_raw\_data.py} 的实际作用：在不丢失主要时间结构的前提下，把原始喷雾轨迹平滑到适合后续建模的程度。

\begin{figure}[t]
\centering
\includegraphics[width=0.92\linewidth]{images/representative_clean_fit.png}
\caption{来自 \codepath{MLP/synthetic_data/BC20241003_HZ_Nozzle1/cdf/plots_clean/T1.png} 的代表性清洗轨迹与拟合曲线。该图在此作为直接证据，说明轨迹清洗与拟合阶段已经产出了可重复使用的穿深目标。}
\label{fig:clean-fit-example}
\end{figure}

\subsection{Stage-1 MSE 基线}

Stage-1 MSE 基线建立了代表性轨迹均值模型。表~\ref{tab:model-summary} 汇总了三个训练阶段的定量证据。

\begin{figure}[t]
\centering
\includegraphics[width=0.92\linewidth]{images/stage1_loss_curves.png}
\caption{来自 \codepath{MLP/runs\_mlp/stage1\_engineered\_mse\_a\_only\_20260410\_020317/stage1\_loss\_curves.png} 的 Stage-1 训练曲线。}
\label{fig:stage1-loss}
\end{figure}

归档 run \codepath{stage1\_engineered\_mse\_a\_only\_20260410\_020317} 在 526 条代表性轨迹上训练，这些轨迹通过在 $t=\SI{5}{ms}$ 处取每组中位穿深最近邻经拟合级掩码筛选得到。该 run 在第 115 个 epoch 达到最佳验证 MSE（缩放目标）18.94，测试集物理 MAE 为~\SI{8.1}{mm}。预训练坍缩报告确认 $A$ 缩放假设下 \SI{0.5}{ms} 后方差缩减 59.5\%，证实缩放在网络看到任何数据之前已解释了主要的直径与压力依赖性。run 目录包含 \codepath{best\_model\_stage1.pt}、\codepath{scaler\_state.json}、epoch 与迭代损失日志，以及 \codepath{pretrain\_collapse\_summary.png}。

\subsection{Stage-2 NLL 扩展}

Stage-2 run 将训练集扩展至全部 14,594 条经筛选的轨迹，并以异方差 NLL 替换 MSE 目标，从 Stage-1 checkpoint 热启动。

\begin{figure}[t]
\centering
\includegraphics[width=0.92\linewidth]{images/stage2_loss_curves.png}
\caption{来自 \codepath{MLP/runs\_mlp/stage2\_engineered\_nll\_a\_only\_20260410\_020607/stage2\_loss\_curves.png} 的 Stage-2 训练曲线。}
\label{fig:stage2-loss}
\end{figure}

归档 run \codepath{stage2\_engineered\_nll\_a\_only\_20260410\_020607} 在第 66 个 epoch 达到最佳验证 NLL~2.038。在留存测试集上，物理 MAE 为~\SI{9.2}{mm}，均值预测标准差为~\SI{11.4}{mm}，表明模型分配的不确定性大于 MAE——与经筛选数据集中真实的条件依赖变化一致。run 目录包含 \codepath{best\_model\_stage2.pt}，作为蒸馏阶段的教师 checkpoint。

\subsection{蒸馏精化}

蒸馏阶段归档了一个学生模型，该模型在覆盖率充足的时间区域内已针对原始 CDF 观测进行精化，同时在覆盖稀疏区域保持对教师的锚定。

归档 run \codepath{distill\_cdf\_onset\_v2\_20260410\_103413} 在第 55 个 epoch 达到最佳验证复合损失~2.327（验证原始 NLL~2.256，验证 KD KL~0.018，验证起始 BCE~0.308）。在留存测试集上：原始 NLL~2.29，KD KL~0.019，起始 BCE~0.309，导数惩罚可忽略（$P_{d_1}\approx6.5\times10^{-7}$，$P_{d_2}\approx2.3\times10^{-5}$）。run 目录包含 \codepath{best\_model\_refinement.pt}、\codepath{epoch\_loss.csv} 和 \codepath{loss\_curves.png}；训练曲线见图~\ref{fig:distillation-loss}。

\begin{table}[t]
\centering
\begin{tabular}{|L{0.20\linewidth}|L{0.18\linewidth}|L{0.20\linewidth}|L{0.20\linewidth}|}
\hline
阶段 & 数据证据 & 最佳验证结果 & 当前状态 \\
\hline
图像处理与拟合 & \codepath{MLP/synthetic\_data} 下有 10 个 campaign 文件夹和 295 个 clean fitted CSV & 已有代表性 clean/fitted trajectory 图 & 已完成且可审计 \\
\hline
Stage 1 MSE 基线 & 526 条代表性轨迹，组感知划分 & 第 115 epoch 验证 MSE~18.94；测试 MAE~\SI{8.1}{mm} & 已完成，保存 \codepath{best\_model\_stage1.pt} \\
\hline
Stage 2 NLL 扩展 & 14,594 条经筛选轨迹，从 Stage~1 热启动 & 第 66 epoch 验证 NLL~2.038；测试 MAE~\SI{9.2}{mm} & 已完成，保存 \codepath{best\_model\_stage2.pt} \\
\hline
蒸馏精化 & 原始 CDF + 教师 KD，区制相关监督 & 第 55 epoch 验证复合损失~2.327 & 已完成，保存 \codepath{best\_model\_refinement.pt} \\
\hline
原型推理 & 来自 \codepath{MLP/toy\_inference\_from\_run.ipynb} 的归档 notebook 输出 & 均值/标准差预测图及简单重叠计算 & 仅为原型，不是已验证产品 \\
\hline
\end{tabular}
\caption{已完成项目阶段的证据汇总。}
\label{tab:model-summary}
\end{table}

\subsection{原型推理 notebook}

原型 notebook 可以加载归档运行元数据，生成均值/标准差曲线并与 wall/cylinder mask 做简单叠加。但当前证据只足以支撑软件集成概念验证，尚不足以支撑完整撞壁分析框架。

\subsection{结果边界与可复现性状态}

本文中的证据来自三个层级：
\begin{enumerate}
  \item 当前工作区中直接存在的资产，例如清洗 CSV、归档图和运行日志；
  \item 嵌入归档 notebook 中的历史输出；
  \item 归档保存的运行配置与 checkpoint。
\end{enumerate}

三个训练阶段均在工作区中保存了专用可部署 checkpoint：Stage-1 \codepath{best\_model\_stage1.pt}、Stage-2 \codepath{best\_model\_stage2.pt} 及蒸馏 \codepath{best\_model\_refinement.pt}，连同各自的 scaler 状态、训练配置和 epoch 级损失日志。提取、清洗与三阶段 surrogate 管线均已完整归档，可供审计。原型推理 notebook 尚未升级为已验证产品，仍是主要的开放边界。

\section{结论}

本论文记录了一条基于证据的工作流，用于处理高速 Mie 散射喷雾视频，将其转换为面向穿深的工程观测量，并在这些观测量之上构建完整的三阶段 surrogate 模型。已完成的贡献包括：
\begin{itemize}
  \item \texttt{GUI.py} 中的手动喷嘴中心标定，
  \item \texttt{main.py} 中的批量逐 plume 特征提取，
  \item \codepath{MLP/fit\_raw\_data.py} 中的轨迹清洗与降阶拟合，
  \item 将稀疏压力与直径输入替换为 Hiroyasu--Arai 缩放群 $A=(\Delta P/\rho_a)^{1/4}d_n^{1/2}$ 的 v2 工程化特征架构（经预训练坍缩检验验证，\SI{0.5}{ms} 后方差缩减 59.5\%），
  \item 已完成的 Stage-1 确定性 MSE 基线（第 115 epoch 验证 MSE~18.94，测试 MAE~\SI{8.1}{mm}），含组感知划分与保存 checkpoint，
  \item 已完成的 Stage-2 异方差 NLL 扩展（第 66 epoch 验证 NLL~2.038，测试 MAE~\SI{9.2}{mm}，均值预测标准差~\SI{11.4}{mm}），其 checkpoint 作为蒸馏教师，以及
  \item 已完成的知识蒸馏精化阶段（第 55 epoch 验证复合损失~2.327），通过区制相关监督将早期时间预测对齐至原始 CDF 观测，同时通过教师 KL 锚定保持晚期时间的稳定性。
\end{itemize}

三阶段 surrogate 管线作为归档工程工件已经完整。物理 $A$ 缩放假设经预训练坍缩检验的实证验证，而区制相关蒸馏框架提供了一种原则性方法，在不被右截断晚期时间观测拉偏的情况下利用原始 CDF 数据。尚未完成的是从归档原型到生产部署的过渡：针对明确实验或仿真目标的撞壁预测验证、完整的二维包络重建以及多次运行可复现性研究，是自然的后续开发优先事项。

\thesisbibliography

\ifXeTeX
\else
\end{CJK*}
\fi
\end{document}
